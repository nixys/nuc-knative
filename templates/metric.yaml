{{- range $index, $item := .Values.metrics }}
---
{{ include "nuc-knative.renderResource" (dict
  "root" $
  "item" $item
  "kind" "Metric"
  "defaultApiVersion" $.Values.apiVersions.metric
  "resourceKey" (printf "metrics[%d]" $index)
  "namespaced" true
) }}
{{- end }}
