{{- range $name, $item := .Values.metrics }}
---
{{ include "nuc-knative.renderResource" (dict
  "root" $
  "item" $item
  "resourceName" $name
  "kind" "Metric"
  "defaultApiVersion" $.Values.apiVersions.metric
  "resourceKey" "metrics"
  "namespaced" true
) }}
{{- end }}
