{{- range $name, $item := .Values.routes }}
---
{{ include "nuc-knative.renderResource" (dict
  "root" $
  "item" $item
  "resourceName" $name
  "kind" "Route"
  "defaultApiVersion" $.Values.apiVersions.route
  "resourceKey" "routes"
  "namespaced" true
) }}
{{- end }}
