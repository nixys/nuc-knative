{{- range $index, $item := .Values.certificates }}
---
{{ include "nuc-knative.renderResource" (dict
  "root" $
  "item" $item
  "kind" "Certificate"
  "defaultApiVersion" $.Values.apiVersions.certificate
  "resourceKey" (printf "certificates[%d]" $index)
  "namespaced" true
) }}
{{- end }}
