{{- range $name, $item := .Values.clusterDomainClaims }}
---
{{ include "nuc-knative.renderResource" (dict
  "root" $
  "item" $item
  "resourceName" $name
  "kind" "ClusterDomainClaim"
  "defaultApiVersion" $.Values.apiVersions.clusterDomainClaim
  "resourceKey" "clusterDomainClaims"
  "namespaced" false
) }}
{{- end }}
